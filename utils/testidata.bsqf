# luku kertoo jotain

luku = random(0, 5);

if (luku > 2) {
    hint("yli");
    hint("poo");
} elif (luku < 2) {
    n = 0;
    while (n < 4) {
        hint(n);
        n = n + 1;
    }
} else {
    hint("oikein")
}
hint("kek")